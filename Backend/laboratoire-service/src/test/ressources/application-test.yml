spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            enabled: false

keycloak:
  enabled: false
  auth-server-url: http://localhost:8080/auth
  realm: test-realm
  resource: test-client
  public-client: true
  principal-attribute: preferred_username